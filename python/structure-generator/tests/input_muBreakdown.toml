target_program = 'muBreakdown'

[common_config]
nx = 128
ny = 128
nz = 60  # 三维模型的网格大小
ptclnum = 30  # 粒子数量

[parameters]
rr = 5  # 核的半径
shell_thickness = 2  # 壳层厚度
iseed = 42  # 随机数种子

[output]
output_file = "output_structure.txt"