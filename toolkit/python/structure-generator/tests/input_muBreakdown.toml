target_program = 'muBreakdown'

[common_config]
nx = 100
ny = 100
nz = 50  # 三维模型的网格大小
ptclnum = 20  # 粒子数量


[parameters]
rr = 3  # 核的半径
shell_thickness = 1  # 壳层厚度
iseed = 42  # 随机数种子
structure_type = 1

[output]
output_file = "output_structure.txt"