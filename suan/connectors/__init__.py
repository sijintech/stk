"""STK connectors: map a simulation code's native I/O to STK datasets and cases.

The protocols are in ``suan.connectors.api`` (standard library only). Built-in
connectors and the entry-point registry arrive in Milestone 1 Phase B.
"""
