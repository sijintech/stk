"""STK data model and formats (docs/specs/stk-data-format-v1.md).

``suan.data.model`` imports NumPy lazily, so descriptors can be built and read
where NumPy is not installed (hub, Windows client).
"""
