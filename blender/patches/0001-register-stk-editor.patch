--- a/source/blender/makesdna/DNA_space_enums.h
+++ b/source/blender/makesdna/DNA_space_enums.h
@@ -1179,9 +1179,10 @@
   SPACE_CLIP = 20,
   SPACE_TOPBAR = 21,
   SPACE_STATUSBAR = 22,
-  SPACE_SPREADSHEET = 23
-
-#define SPACE_TYPE_NUM (SPACE_SPREADSHEET + 1)
+  SPACE_SPREADSHEET = 23,
+  SPACE_STK = 24,
+
+#define SPACE_TYPE_NUM (SPACE_STK + 1)
 };
 
 /* use for function args */
--- a/source/blender/makesdna/DNA_space_types.h
+++ b/source/blender/makesdna/DNA_space_types.h
@@ -107,6 +107,22 @@
 /** \name Space Info
  * \{ */
 
+struct SpaceSTK_Runtime;
+
+/** STK native scientific workbench; service credentials are never DNA data. */
+struct SpaceSTK {
+  SpaceLink *next = nullptr, *prev = nullptr;
+  ListBaseT<ARegion> regionbase = {nullptr, nullptr};
+  char spacetype = SPACE_STK;
+  eSpace_Link_Flag link_flag = {};
+  char _pad0[6] = {};
+  float panels[4] = {0.23f, 0.28f, 0.22f, 0.43f};
+  float yaw = 30.0f, pitch = 55.0f, zoom = 0.85f;
+  float pan[2] = {0.0f, 0.0f};
+  char _pad1[4] = {};
+  SpaceSTK_Runtime *runtime = nullptr;
+};
+
 /** Info Header. */
 struct SpaceInfo {
   SpaceLink *next = nullptr, *prev = nullptr;
--- a/source/blender/editors/include/ED_space_api.hh
+++ b/source/blender/editors/include/ED_space_api.hh
@@ -33,6 +33,7 @@
 void ED_spacetype_node();
 void ED_spacetype_buttons();
 void ED_spacetype_info();
+void ED_spacetype_stk();
 void ED_spacetype_file();
 void ED_spacetype_action();
 void ED_spacetype_nla();
--- a/source/blender/editors/space_api/spacetypes.cc
+++ b/source/blender/editors/space_api/spacetypes.cc
@@ -74,6 +74,7 @@
   ED_spacetype_node();
   ED_spacetype_buttons();
   ED_spacetype_info();
+  ED_spacetype_stk();
   ED_spacetype_file();
   ED_spacetype_action();
   ED_spacetype_nla();
--- a/source/blender/editors/CMakeLists.txt
+++ b/source/blender/editors/CMakeLists.txt
@@ -39,6 +39,7 @@
   add_subdirectory(space_graph)
   add_subdirectory(space_image)
   add_subdirectory(space_info)
+  add_subdirectory(space_stk)
   add_subdirectory(space_nla)
   add_subdirectory(space_node)
   add_subdirectory(space_outliner)
--- a/source/blender/makesrna/intern/rna_space.cc
+++ b/source/blender/makesrna/intern/rna_space.cc
@@ -145,6 +145,7 @@
      "Python Console",
      "Interactive programmatic console for "
      "advanced editing and script development"},
+    {SPACE_STK, "STK", ICON_EXPERIMENTAL, "STK", "Scientific computing workbench"},
     {SPACE_INFO, "INFO", ICON_INFO, "Info", "Log of operations, warnings and error messages"},
     /* Special case: Top-bar and Status-bar aren't supposed to be a regular editor for the user. */
     {SPACE_TOPBAR,
@@ -773,6 +774,8 @@
       return RNA_SpaceFileBrowser;
     case SPACE_IMAGE:
       return RNA_SpaceImageEditor;
+    case SPACE_STK:
+      return RNA_SpaceSTK;
     case SPACE_INFO:
       return RNA_SpaceInfo;
     case SPACE_SEQ:
@@ -8211,6 +8214,13 @@
   RNA_api_space_filebrowser(srna);
 }
 
+static void rna_def_space_stk(BlenderRNA *brna)
+{
+  StructRNA *srna = RNA_def_struct(brna, "SpaceSTK", "Space");
+  RNA_def_struct_sdna(srna, "SpaceSTK");
+  RNA_def_struct_ui_text(srna, "STK", "Native scientific computing workbench");
+}
+
 static void rna_def_space_info(BlenderRNA *brna)
 {
   StructRNA *srna;
@@ -9543,6 +9553,7 @@
   rna_def_space_console(brna);
   rna_def_console_line(brna);
   rna_def_space_info(brna);
+  rna_def_space_stk(brna);
   rna_def_space_userpref(brna);
   rna_def_node_tree_path(brna);
   rna_def_space_node(brna);
--- a/source/blender/windowmanager/intern/wm_draw.cc
+++ b/source/blender/windowmanager/intern/wm_draw.cc
@@ -577,7 +577,7 @@
 
 static bool wm_region_use_viewport_by_type(short space_type, short region_type)
 {
-  return (ELEM(space_type, SPACE_VIEW3D, SPACE_IMAGE, SPACE_NODE) &&
+  return (ELEM(space_type, SPACE_VIEW3D, SPACE_IMAGE, SPACE_NODE, SPACE_STK) &&
           region_type == RGN_TYPE_WINDOW) ||
          ((space_type == SPACE_SEQ) && ELEM(region_type, RGN_TYPE_PREVIEW, RGN_TYPE_WINDOW));
 }
@@ -602,6 +602,7 @@
     SPACE_NAME(SPACE_FILE);
     SPACE_NAME(SPACE_IMAGE);
     SPACE_NAME(SPACE_INFO);
+    SPACE_NAME(SPACE_STK);
     SPACE_NAME(SPACE_SEQ);
     SPACE_NAME(SPACE_TEXT);
     SPACE_NAME(SPACE_ACTION);
--- a/source/blender/editors/space_api/CMakeLists.txt
+++ b/source/blender/editors/space_api/CMakeLists.txt
@@ -29,6 +29,7 @@
   bf_editor_space_graph
   bf_editor_space_image
   bf_editor_space_info
+  bf_editor_space_stk
   bf_editor_space_nla
   bf_editor_space_node
   bf_editor_space_outliner
--- a/source/blender/python/intern/bpy_rna_callback.cc
+++ b/source/blender/python/intern/bpy_rna_callback.cc
@@ -215,6 +215,9 @@
   }
   if (srna == RNA_SpaceImageEditor) {
     return SPACE_IMAGE;
+  }
+  if (srna == RNA_SpaceSTK) {
+    return SPACE_STK;
   }
   if (srna == RNA_SpaceInfo) {
     return SPACE_INFO;
diff --git a/scripts/presets/keyconfig/keymap_data/blender_default.py b/scripts/presets/keyconfig/keymap_data/blender_default.py
--- a/scripts/presets/keyconfig/keymap_data/blender_default.py
+++ b/scripts/presets/keyconfig/keymap_data/blender_default.py
@@ -8981,6 +8981,10 @@
     if params is None:
         params = Params()
     return [
+        ("STK", {"space_type": "STK", "region_type": "WINDOW"}, {"items": [
+            ("stk.interact", {"type": kind, "value": "PRESS", "any": True}, None)
+            for kind in ("LEFTMOUSE", "MIDDLEMOUSE", "WHEELUPMOUSE", "WHEELDOWNMOUSE")
+        ]}),
         # Window, screen, area, region.
         km_window(params),
         km_screen(params),
